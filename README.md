# CSUB Smart Campus Map

This is a team project built with Expo (React Native) and a Node.js backend.

## Project Structure
- apps/mobile      → Expo mobile app (frontend)
- backend/server   → Node.js + Express backend
- shared/types     → Shared TypeScript models

## Prerequisites
- Node.js 18+
- npm
- Expo Go app (mobile) or Android emulator

## Run Frontend (Expo)
```bash
cd apps/mobile
npm install
npx expo start
